# API base URL for production builds.
# Local development can keep this unset to use Vite proxy (/api).
# On Vercel, set VITE_API_BASE_URL to your backend origin.
VITE_API_BASE_URL=http://157.230.254.81:8001
